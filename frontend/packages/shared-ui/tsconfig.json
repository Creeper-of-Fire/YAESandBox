{
  "extends": "../../tsconfig.base.json", // 继承根目录的基础配置
  "compilerOptions": {
    // --- 包的特定配置 ---
    "baseUrl": ".", // baseUrl 必须在这里定义
    "paths": {
      "@/*": ["src/*"] // 路径别名也在这里定义
    },
    "emitDeclarationOnly": false,
    "outDir": "dist", // 库需要指定输出目录
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    // 如果 shell 是应用而不是库，可以不生成声明文件
    "declaration": true,
    "declarationMap": true
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [
    // 如果 shell 依赖其他 monorepo 内的包，在这里引用
    // 这能让 VS Code/WebStorm 立即知道它们之间的关系
//    { "path": "../packages/core-services" },
//    { "path": "../plugins/workbench" }
    // ... etc
  ]
}